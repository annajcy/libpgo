-- Unreleased
  - replace the legacy MassField hierarchy with VolumeDensity and ShellArealDensity; migrate ShellDensityThickness to ShellArealDensity.from_density_thickness and ShellDensityElasticThickness to ShellArealDensity.from_elastic_parameter
  - replace Torch equilibrium layer objective_energy with additional_energy; the additional term must be independent of the optimized material field
  - validate density derivative buffer spans against the referenced parameter layout; parameter-independent sources now require an empty span
  - remove the obsolete Markdown/notebook example-test harness and notebook-only development dependency
  - remove the parameter-omitting DeformationModel::prepareData overload; parameterized models now reject missing elastic or plastic parameter buffers
  - replace the pgo scheduling facade with explicit GlobalTbbControl, ThreadingPolicy, and ArenaThreadingExecutor primitives
  - migrate first-party loops, reductions, and sorting to native oneTBB algorithms
  - remove the redundant parallelFor3D wrapper in favor of call-site-owned row/block decomposition
  - remove SuiteSparse/OpenMP and non-Accelerate BLAS dependency stacks from libpgo
  - route supported Eigen dense operations to system Accelerate BLAS and keep other Eigen kernels serial on macOS
  - scope and restore per-thread MKL or Accelerate policies for every controlled arena participant
  - align direct, crossover, control-matrix, and VTune MKL benchmarks on executor-owned arena and TLS-budget policies
  - keep parallel controls opt-in: Newton executes directly while applications and benchmarks may use explicit executors

-- v0.0.3
  - fix various bug in cmake
  - fix bugs about misusage of eigen library
  - adding alignment of eigen library
  - fix bugs in simulator tools
  - improve Newton solver (adding damping, improve line search, etc.)
  - improve finite difference library
  - substantially improve solid deformation models
  - add koiter shell models
  - add tetgen tool
  - add msh conversion tool (require gmesh)
  - add conversion tool that convert animation sequence to Alembic  (require Alembic)
  - add new examples
  - add small meshing tools
  - 

-- v0.0.2b

- Support building wheel, and the prebuilt wheel are in ./dist folder. The following platforms have been prebuilt
  - windows 11, visual studio 2022 wheel
  - ubuntu 24.04 wheel

- Support subprojects better
  - support subprojects that include python module

- Cleanups, bugs
  - fix MKL incorrect linked
  - some missing copyright info
  - add dependency in setup.py
  - gmp mpfr update
  - MKL 2025 incompatible with eigen

-- v0.0.2

- Add more functionalities in the direction of geometry processing
  - libigl support
  - geogram support
  - tetgen support
  - add a tool that can call several remeshers in cgal and geogram

- clean up bugs, errors, unnecessary parts
  - bugs in pgo python binding
  - bugs in lagrangian
  - cleanup third-party unnecessary code  
  - remove unnecessary code in python
  - add .clang-format
  - restructure code for the latter usage
    cpp -> core
  - add supports to new projects that is based on libpgo. Users can clone them into projects folder to make it compile
  - further polish cmake 
  - remove unnecessary in tools
  - when compiled with openmp on linux for suitesparse, the gomp lib has conflicts with MKL, casuing pardiso not working correctly.

-- v0.0.1

- Initial push
